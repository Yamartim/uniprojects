#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    //conta par impar
    /*

    int n, par, impar;
    par=0;
    impar=0;

    scanf("%d", &n);

    while(n!=0){

        if(n%2==0)
            par++;
        else
            impar++;

        scanf("%d", &n);

    }

    printf("\n\n%d pares\n%d impares", par, impar);
    */

    // equaco de peli: raiz inteira(incompleto)

    /*
    int sub, n, aux;
    sub-1;

    scanf("%d", &n);
    aux=n

    while(aux>sub){

        aux-=sub;
        sub+=2;

    }

    printf("%d", aux);
    */

    //nome formatado (incompleto)

    /*
    char nome[50], correto[50];
    int i;

    gets(nome);
    while (i] != '\0') {
      if (s[c] >= 'A' && s[c] <= 'Z') {
         s[c] = s[c] + 32;
      }
      c++;
   }

    for(i=1; i<50; i++){
        if(correto[i-1]==' ')
            correto[i]-=32;

    }

    printf("\n%d", correto)*/

    //palindromo(krl muito complexo nao deu

    /*
    int i, j, palindromo;
    palindromo=1;
    char str[30];

    gets(str);

    j=strlen(str)
    for(i=0; i<=strlen(str); i++){
        //if (str[i] = ' ')
        //    i++;

        // if (str[j] = ' ')
        //    j++;
        if(str[i]!=str[j]){
            palindromo=0;
        }
        j--
    }

    if(palindromo)
        printf("eh palindromo");
    else
        printf("nao eh palindromo");

    */

    //raiz quadrada metodo babilonico(ou de newton)
}
